(() => { const input = document.querySelector('#avatar-input'); const preview = document.querySelector('#profile-preview'); input?.addEventListener('change', () => { const file = input.files[0]; if (!file || !file.type.startsWith('image/')) return; const url = URL.createObjectURL(file); if (preview.tagName === 'IMG') preview.src = url; else { const image = document.createElement('img'); image.id = 'profile-preview'; image.className = 'profile-photo'; image.alt = 'Vista previa de foto'; image.src = url; preview.replaceWith(image); } }); })();
